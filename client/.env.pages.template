# Copy this to a local Pages-only env file if you want to test a hosted frontend build.
# Cloudflare Pages should set this as an environment variable in the dashboard.
VITE_API_BASE_URL=https://your-backend.onrender.com/api
